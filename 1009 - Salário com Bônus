#include <stdio.h>
 
int main() {
	
	char nome[50];
	double salario;
	double vendas; 
	double comissao;
	
	gets(nome);
	scanf("%lf", &salario);
	scanf("%lf", &vendas);
	 
    comissao=vendas*0.15;
    printf("TOTAL = R$ %.2lf\n", salario+comissao);    
    
    return 0;
}
